query GetAllVideosTestimoniales {
  allVideos(tags: "Testimoniales", limit: 4) {
    __typename
    items {
      id
      __typename
      name
      poster
      mobilePoster
      url
      Tags {
        id
        __typename
        name
      }
    }
  }
}
