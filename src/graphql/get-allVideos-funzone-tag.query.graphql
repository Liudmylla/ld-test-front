query GetAllVideosFunzone {
  allVideos(tags: "Funzone", limit: 4) {
    __typename
    items {
      id
      __typename
      name
      poster
      mobilePoster
      url
      Tags {
        id
        __typename
        name
      }
    }
  }
}
